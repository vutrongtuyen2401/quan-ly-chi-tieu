# V2 — Token / Quota Optimized

Adds progressive disclosure, task routing, search-before-read, verification budgets, context compression, and minimal task templates.

The system intentionally does not install giant third-party skill collections. Use community repos as references and adopt only relevant patterns after review.

The goal is to avoid unnecessary context loading and repeated work, not to suppress necessary reasoning or testing.
