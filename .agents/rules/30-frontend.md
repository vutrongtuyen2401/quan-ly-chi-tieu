# Frontend Rules
Stack: Vue 3 + Vite + Axios + Chart.js/vue-chartjs.
Avoid broad `App.vue` rewrites.
Preserve existing UI behavior unless UI change is requested.
Handle loading, success, empty, and error states.
Never display fabricated financial data after API/AI failure.
Run the frontend build for frontend-affecting changes.
