---
name: regression-testing
description: Use after changes. Start from changed code and its callers, run targeted checks first, expand by dependency/risk, distinguish environment failures from task-caused failures, and report evidence.
---

# Regression Testing

Use after changes. Start from changed code and its callers, run targeted checks first, expand by dependency/risk, distinguish environment failures from task-caused failures, and report evidence.

Follow the project development protocol and load only the context needed for this skill.