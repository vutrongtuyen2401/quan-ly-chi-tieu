---
name: database-change
description: Use for schema/migration/data changes. Inspect schema, foreign keys, indexes, reads/writes, migration conventions, and tests. Preserve existing data and verify integrity.
---

# Database Change

Use for schema/migration/data changes. Inspect schema, foreign keys, indexes, reads/writes, migration conventions, and tests. Preserve existing data and verify integrity.

Follow the project development protocol and load only the context needed for this skill.