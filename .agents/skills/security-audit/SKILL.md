---
name: security-audit
description: Use for auth/authorization/security. Trace client→endpoint→auth→resource→ownership→DB→response. Check IDOR, stale JWT state, leakage, secrets, unsafe fallbacks, and add regression tests.
---

# Security Audit

Use for auth/authorization/security. Trace client→endpoint→auth→resource→ownership→DB→response. Check IDOR, stale JWT state, leakage, secrets, unsafe fallbacks, and add regression tests.

Follow the project development protocol and load only the context needed for this skill.